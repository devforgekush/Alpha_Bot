# Optional voice stack for group calls
# These packages require build tools and native dependencies in some cases.
ntgcalls==2.0.5
pytgcalls==2.1.0
# pin tgcalls to an available build; pip shows only 3.0.0.dev6
tgcalls==3.0.0.dev6
